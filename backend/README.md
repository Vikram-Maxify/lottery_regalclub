# MongoDB converted backend

1. npm install
2. Set MONGO_URI in .env
3. Set mail/SunPay credentials in .env
4. node index.js

All SQL/mysql2 queries and mysql connection have been removed. Data uses Mongoose models.
